<div class="container">
  <h1>Add a New List</h1>
  <%= form_with model: @list do |f| %>
    <%= f.label :name %>
    <%= f.text_field :name %>
    <%= f.submit%>
  <% end %>

  <%= link_to "Back to lists", lists_path %>
</div>
